## Process this file with automake to produce Makefile.in

if BUILD_BROKEN
noinst_PROGRAMS = proctest
endif

proctest_SOURCES = proctest.cc

proctest_LDADD = $(top_builddir)/coda-src/util/libutil.la

