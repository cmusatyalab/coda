# TARGETS:
#	install:
#		CODA_ASSERT.h -> $(INCLSPECIAL)
#	 all:
#		no op

TOPDIR = @top_srcdir@

srcdir = @srcdir@
VPATH  = @srcdir@
TOPOBJ = @TOPOBJ@

include $(TOPDIR)/Makeconf

HEADERS = errno.h nlist.h
SUBDIRS = sys

ifeq ($(SYS),cygwin32)
LIBRARIES = libbraindead.a
OBJECTS = braindead.o
libbraindead.a: $(OBJECTS)
endif

include $(TOPDIR)/configs/Makerules
