# TARGETS:
#	install:
#		CODA_ASSERT.h -> $(INCLSPECIAL)
#	 all:
#		no op

TOPDIR = @top_srcdir@

srcdir = @srcdir@
VPATH  = @srcdir@
TOPOBJ = @TOPOBJ@

include $(TOPDIR)/Makeconf

HEADERS = errno.h nlist.h stdlib.h 
SUBDIRS = sys

include $(TOPDIR)/configs/Makerules

ifeq ($(SHORTSYS),cygwin)
LIBRARIES = libbraindead.a
OBJECTS = braindead.o
libbraindead.a: $(OBJECTS)
endif
