# Coda remote procedure call package
sdesc: "A remote procedure call package used by Coda"
ldesc: "Yet another remote procedure call package.  This one has
a 'side effect' feature.  Most visible one is the sftp side effect
that is used by Coda to transfer files as a side effect of a
rpc."
curr: 2.6.1-1
category: Libs
requires: lwp rvm

