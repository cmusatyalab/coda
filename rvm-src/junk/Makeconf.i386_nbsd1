# system-specific Makeconf for i386_nbsd1 platform

SYS=i386_nbsd1

# Some programs use insque and deque, which are in libcompat.a
LIBCOMPAT=/usr/lib/libcompat.a

SYS_INCLFLAGS=
SYS_CFLAGS=
SYS_LIBFLAGS=
